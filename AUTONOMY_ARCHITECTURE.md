# 此间自主唤醒与透明行动链

这份文档是后续开发的硬约束，不是一次性的构想记录。目标是让 AI 能在“此间”里按设定周期醒来，观察当天发生的事，自行决定是否思考、发消息、发朋友圈或写日记，同时让用户能够完整查看它为什么醒来、看到了什么、做了什么以及花费了多少。

## 一、产品边界

- 聊天中的“思考过程”直接读取中转站返回的 `reasoning_content`、`reasoning`、`thinking` 或 Anthropic `thinking_delta`，不额外伪造摘要。
- 自主唤醒不能依赖手机网页定时器。浏览器或 PWA 退到后台后会被系统冻结，可靠调度必须放在持续运行的服务端进程中。
- AI 可以自行选择“不行动”。一次唤醒不应被迫发消息或制造内容。
- 日记、朋友圈、聊天、通知和记忆仍使用各自的产品界面，但底层必须经过同一条事件与行动链。

## 二、不可破坏的链路

任何会改变此间状态的功能都必须经过：

```text
用户操作 / 定时唤醒
  → ContextSnapshot（当时看到了什么）
  → ThinkingRun（中转站返回的思考过程）
  → Decision（决定做或不做什么）
  → DomainAction（统一执行动作）
  → EventLog（结果、用量、金额与错误）
  → 对应页面更新
```

禁止让页面直接写入另一套孤立数据。以后新增或修改聊天、日记、朋友圈、通知、记忆等功能时，必须同步检查：

1. 这个变化是否产生领域事件。
2. AI 自主行动是否能调用同一个动作函数。
3. 事件是否进入当天上下文。
4. 思考过程、结果、Token 和金额是否能在日志中追溯。
5. 失败或预算不足是否留下明确记录，而不是静默消失。

建议的统一动作名：

- `chat.send`
- `diary.write`
- `diary.update`
- `moment.publish`
- `notification.send`
- `memory.propose`
- `autonomy.noop`

## 三、服务端模块

### WakeScheduler

- 用户可设置唤醒间隔，例如 15 分钟、30 分钟、1 小时或自定义。
- 保存 `nextWakeAt`，服务重启后能够恢复，而不是重新从零计时。
- 支持安静时段、暂停、立即唤醒和错过任务的处理策略。

### ContextSnapshot

每次醒来只构造当时可见的快照，并保存引用：

- 今天的聊天增量
- 今日日记与情绪、经期记录
- 今日朋友圈
- 最近重要记忆
- 上一次自主运行的决定与未完成事项
- 当前时间、预算余额和允许的动作

### ThinkingRun

- 使用独立的自主提示词，不与普通聊天提示词混在一起。
- 保存中转站实际返回的思考字段和最终决策。
- 决策输出使用结构化数据，动作执行器不从自然语言里猜命令。

建议的决策结构：

```json
{
  "state": "reflecting",
  "decision": "publish_moment",
  "reason": "今天的日记里记录了一件值得共同保存的小事",
  "actions": [
    { "type": "moment.publish", "payload": { "content": "..." } }
  ]
}
```

### ActionExecutor

- 自主行动与用户点击必须复用相同的领域函数。
- 每个动作有 `pending / succeeded / failed / blocked` 状态。
- 写操作使用幂等键，服务重试时不能重复发朋友圈或重复写日记。

### BudgetLedger

至少提供四道硬限制：

- 单次唤醒最大 Token
- 每日最大 Token
- 每日最大人民币金额
- 每月最大人民币金额

中转站通常只返回 Token，不一定返回人民币费用，因此需要按模型配置输入、输出和思考 Token 的单价。预算在请求前预检，请求后按实际用量结算；超过硬上限后本次运行标记为 `blocked_by_budget`，不得继续行动。

## 四、透明可视化：栖息日志

不建议做成后台控制台式大表格。更适合此间的是一条可展开的纵向“生活轨迹”：

```text
22:30  醒来
       看见：3 条聊天、1 篇日记、2 条朋友圈
       思考：可展开查看中转站返回内容
       决定：给你发一条消息
       行动：消息已送达
       花费：输入 2,104 / 思考 816 / 输出 132 Token · ¥0.08
       回到休眠 · 下次 23:00
```

页面表现建议：

- 首页只显示一枚低调的状态条：`休眠中 · 27 分钟后醒来`。
- 独立“栖息日志”页按天显示纵向时间线。
- 每次唤醒是一张折叠卡，默认只显示时间、决定、动作和花费。
- 展开后依次显示“看见 → 思考 → 决定 → 行动 → 结果”。
- 思考过程使用等宽字体的内嵌纸条，不做成醒目的聊天气泡，避免喧宾夺主。
- 发消息、写日记、发朋友圈使用不同的小图标，但都挂在同一条时间线上。
- 顶部使用一条预算进度条同时显示今日 Token 与人民币额度。
- 支持筛选：全部、只看思考、只看行动、只看失败、只看花费。

## 五、事件数据最小结构

```json
{
  "id": "evt_...",
  "runId": "run_...",
  "occurredAt": "2026-08-03T12:00:00.000Z",
  "actor": "user | ai | system",
  "type": "diary.write",
  "source": "chat | scheduler | diary | moments",
  "contextRefs": ["chat:...", "diary:2026-08-03"],
  "reasoning": "中转站返回的思考字段",
  "payload": {},
  "result": {},
  "usage": {
    "inputTokens": 0,
    "reasoningTokens": 0,
    "outputTokens": 0,
    "costCny": 0
  },
  "status": "succeeded"
}
```

事件日志应追加写入，不原地覆盖。页面当前状态可以由事件投影生成；即使暂时仍用现有本地数据，也要保留迁移到 SQLite、Supabase 或其他持久存储的接口。

## 六、实施顺序

1. 聊天先接通可选的中转站思考字段。
2. 把聊天、日记、朋友圈的写入集中为领域动作，并记录统一事件。
3. 增加只思考、不行动的手动“立即唤醒”模式，验证上下文和预算。
4. 增加服务端定时器、暂停和安静时段。
5. 逐项开放发消息、写日记、发朋友圈等自主动作。
6. 最后再部署到持续在线的 HTTPS 服务；本地电脑关机时，本地 Node 调度器无法继续醒来。

