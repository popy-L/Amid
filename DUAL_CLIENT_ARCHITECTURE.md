# 此间双端架构

此间只有一个产品版本、一个 `main` 分支和一套正式数据。手机与电脑是两个界面壳，不是两份长期分叉的代码。

## 版本边界

- `main` 始终包含可一起发布的 Mobile 与 Desktop。
- 功能分支按改动命名，例如 `codex/mobile-chat-spacing`、`codex/desktop-shell`、`codex/shared-sync`。
- 功能合并后删除分支，不保留永久的 `mobile`、`desktop` 分支。
- 一个发布标签同时描述 `Mobile`、`Desktop`、`Shared`、`Server` 与 `Database` 的变化。

## 客户端边界

当前仍由同一个网页入口启动。`public/platform.js` 只负责识别展示模式和声明统一版本，不复制业务状态。

- Mobile：小于 1100px 或非精细指针设备，保留 PWA 单列、底部导航和触摸交互。
- Desktop：至少 1100px 且有精细指针，启用桌面侧栏和宽屏布局。
- 设备模式只影响布局，不影响聊天、记忆、日记、日历、朋友圈或提示词的数据格式。

CSS 应把手机样式作为基线，并将电脑专属布局放在明确的桌面媒体查询中。不要用 JavaScript 复制两份页面状态。

## 数据边界

计划中的共享正式数据：

- 聊天、附件、收藏、语音与工具链路；
- 四块核心记忆、补充记忆与每日聊天总结；
- 日记、日历、情绪、经期和朋友圈；
- 提示词、服务商资料、模型分配与主题资源。

必须按设备保存的数据：

- 滚动位置、窗口尺寸和桌面侧栏状态；
- 麦克风、扬声器、通知及后台权限；
- Termux/Windows 本地路径和本地服务状态；
- 离线缓存与尚未同步的草稿。

Supabase 将作为共享数据源，IndexedDB 作为每台设备的离线缓存。同步写入需要稳定 UUID、`updated_at`、版本号与冲突策略，不能用整份对象覆盖另一台设备的更新。

密钥不进入 GitHub，也不以明文写进客户端共享表。服务商密钥保留在可信服务端环境变量或加密存储中。

## 双端验证

使用 Responsively App 同时打开手机与电脑尺寸：

- 布局测试可开启镜像点击、滚动与热更新；
- 写入日记、发送消息等功能测试应关闭镜像交互，避免一个动作在两个视口重复提交；
- 修改共享组件或共享数据逻辑时必须同时检查两端；
- 修改设备专属布局时记录影响范围：`mobile`、`desktop`、`shared`、`server` 或 `database`。

